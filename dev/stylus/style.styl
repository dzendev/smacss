// @import '../../node_modules/normalize.css/normalize.css'
@import 'rupture'
@import 'nib'
@import 'jeet'

rupture.scale = 0 400px 600px 800px 1050px 1800px
rupture.scale-names = 'xs' 's' 'm' 'l' 'xl' 'hd'

/*@font-face
	font-family 'NairiNormal'
	font-style normal
	font-weight normal
	src: url('../fonts/NairiNormal.eot')
	src: url('../fonts/NairiNormal.eot') format('embedded-opentype'), url('../fonts/NairiNormal.woff2') format('woff2'), url('../fonts/NairiNormal.woff') format('woff'), url('../fonts/NairiNormal.ttf') format('truetype'), url('../fonts/NairiNormal.svg#11184') format('svg')*/

@import 'NairiNormal.css'

*
*:before
*:after
	box-sizing border-box

h1
	font-size 30px
	font-family: 'NairiNormal'

p:nth-child(1)
	color blue
	padding-bottom:20px

.wrap_icon
	background: #000

*
	padding: 0
	margin: 0

img
	max-width 100%
	height auto

.content
	clearfix()
.col
	span(1/2)
	+below(m)
		column(1/3)
// .el
// 	clearfix()
// 	display flex
// 	+below(m)
// 		display block

@import '_sprite.styl'

spriteIcon($sprite)
	background-position: $sprite[2] $sprite[3]
	background-image: url($sprite[8])
	width: $sprite[4]
	height: $sprite[5]

spriteIconHover($sprite)
	background-position: $sprite[2] $sprite[3]

spritesIcon($sprites)
	for $sprite in $sprites
		$sprite_name = $sprite[9]
		if match('__hover', $sprite_name)
			$sprite_name = replace('__hover', ':hover', $sprite_name)
			.icon-{$sprite_name}
				spriteIconHover($sprite)
		else
			.icon-{$sprite_name}
				spriteIcon($sprite)

.icon
	display inline-block

spritesIcon($spritesheet_sprites)

.icon-svg
	display inline-block
	width: 64px
	height: 64px

@import 'css/sprite.css'

@import '_svg-font.css'